
<nav class="navbar navbar-expand-md navbar-light bg-light">
    <div class="navbar-collapse collapse w-100 order-1 order-md-0 dual-collapse2">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link nav-link-left hvr-underline-from-center border-left-2 border-right border-dark" href="{{ path("home") }}">Home</a>
            </li>
            {% if is_granted('ROLE_USER') and not is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link nav-link-left hvr-underline-from-center border-left border-right border-dark" href="{{ path("media_list") }}">Browse</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-left hvr-underline-from-center border-left border-right-2 border-dark" href="{{ path("myMedia_list") }}">My Medias</a>
                </li>
            {% elseif is_granted('ROLE_ADMIN')%}
                <li class="nav-item">
                    <a class="nav-link nav-link-left hvr-underline-from-center border-left border-right-2 border-dark" href="{{ path("admin_menu") }}">Manage</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link nav-link-left hvr-underline-from-center border-left border-right-2 border-dark" href="{{ path("admin_menu") }}">Browse</a>
                </li>
            {% endif %}

        </ul>
    </div>
    <div class="mx-auto order-0">
        <a class="navbar-brand mx-auto" href="{{ path("home") }}">
            <img src="{{ asset('img/Rocking_chair_brand.png') }}" height="75" alt="Rockin' Chair">
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target=".dual-collapse2">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
    <div class="navbar-collapse collapse w-100 order-3 dual-collapse2 ">
        {% if is_granted('ROLE_USER') %}
            <ul class="navbar-nav ml-auto ">
                <div class="dropdown navbar-nav">
                    <div class="nav-item hvr-underline-from-center dropdown-toggle border-left-2 border-right border-dark pt-2 px-2" id="dropdownMenuButton" data-toggle="dropdown">
                        <img src="{{ asset('img/user.png') }}" width="22" alt="user-icon">
                        {{ app.user.username }}
                    </div>

                    <div class="dropdown-menu text-center" aria-labelledby="dropdownMenuButton">
                        {% if not is_granted('ROLE_ADMIN') %}
                            <a class="nav-link border-dark hvr-underline-from-center" href="{{ path("user_account") }}">Account</a>
                            <a class="nav-link border-dark hvr-underline-from-center" href="{{ path("myMedia_list") }}">My List</a>
                        {% else %}
                            <a class="nav-link border-dark hvr-underline-from-center" href="{{ path("admin_menu") }}">Manage</a>
                        {% endif %}
                    </div>
                </div>

                </li>
                <li class="nav-item hvr-underline-from-center">
                    <a class="nav-link border-left border-right-2 border-dark text-danger" href="{{ path("logout") }}">Logout</a>
                </li>
            </ul>
        {% else %}
            <ul class="navbar-nav ml-auto ">
                <li class="nav-item hvr-underline-from-center">
                    <a class="nav-link border-left-2 border-right border-dark text-success" href="{{ path("login") }}">Login</a>
                </li>
                <li class="nav-item hvr-underline-from-center ">
                    <a class="nav-link border-left border-right-2 border-dark text-dark" href="{{ path("register") }}">Register</a>
                </li>
            </ul>
        {% endif %}

    </div>
</nav>